@model IEnumerable<LaborU.Models.ViewModels.UserViewModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<table class="table table-striped table-bordered">
    <thead>
    <tr>
        <th>Email</th>
        <th>姓名</th>
        <th>地址</th>
        <th>身份證字號</th>
    </tr>
    </thead>
    @foreach (var userViewModel in Model)
    {
        <tr>
            <td>@Html.Raw(userViewModel.Email)</td>
            <td>@Html.Raw(userViewModel.ContactIds.Count())</td>
            @* <td>@Html.Raw(userViewModel.Address)</td> *@
            @* <td> *@
            @*     <input type="password" readonly="readonly" value="@Html.Raw(userViewModel.IDNumber)" /> *@
            @* </td> *@
            @* <td> *@
            @*     <a href="@Url.Action("Edit", new {Id = userViewModel.Id})" asp> *@
            @*         <button type="button" class="btn btn-primary">編輯</button> *@
            @*     </a> *@
            @* </td> *@
        </tr>

    }
</table>